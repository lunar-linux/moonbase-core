default_pre_build &&

sedit "s/-O3/$CFLAGS/" unix/configure unix/Makefile &&
sedit "s/MANDIR = \$(prefix)\/man/MANDIR = \$(prefix)\/share\/man/" unix/Makefile &&
sedit 's@/usr/local@${DESTDIR:-}/usr@' unix/configure &&
sedit 's@/usr/local@$(DESTDIR)/usr@g' unix/Makefile &&

cp -rf unix/* .
