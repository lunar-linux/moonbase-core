depends zlib
depends automake
depends libtool
depends %OSSL

optional_depends xz \
                 "--with-xz" \
                 "--without-xz" \
                 "support for xz compressed modules" y

optional_depends gtk-doc \
                 "--enable-gtk-doc" \
                 "--disable-gtk-doc" \
                 "for building documentation" n
