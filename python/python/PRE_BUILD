default_pre_build &&

sedit 's|{INSTALL} -m 555|{INSTALL} -m 755|' Makefile.pre.in &&

# Fix for --with-system-libmpdec with mpdecimal 2.5
sedit '/#include "mpdecimal.h"/a #define UNUSED __attribute__((unused))' Modules/_decimal/_decimal.c &&

# Ensure that we link against system libraries
rm -rf Modules/{expat,zlib} &&
rm -rf Modules/_ctypes/{darwin,libffi}*
