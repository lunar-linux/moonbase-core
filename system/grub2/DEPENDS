depends ncurses
depends gettext
depends bison
depends xz

optional_depends os-prober "" "" "for OS auto-detection on other partitions" y
optional_depends lvm2 "--enable-device-mapper" "--disable-device-mapper" "enable Linux device-mapper support" y
optional_depends fuse "--enable-grub-mount" "--disable-grub-mount" "build and install the grub-mount utility"
optional_depends freetype2 "--enable-grub-mkfont" "--disable-grub-mkfont" "for grub mkfont support"
optional_depends efibootmgr "" "" "for grub-install EFI support" y
